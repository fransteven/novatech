"use client";

import { CashAccountWithBalance } from "@/services/cash-service";

export function TransferDialog({ accounts }: { accounts: CashAccountWithBalance[] }) {
  return <button data-stub="transfer-dialog">Transferir</button>;
}
