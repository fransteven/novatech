import { CashAccountWithBalance } from "@/services/cash-service";

export function CashAccountsGrid({ accounts }: { accounts: CashAccountWithBalance[] }) {
  return <div data-stub="cash-accounts-grid" />;
}
